function calc() {
    console.log(document.getElementById("product"));
  }
  function calc() {
    const price = document.getElementById("product").value;
    const number = document.getElementById("number").value;
    window.alert(`${price}The circle is.${number}Pieces. Subtotal.${price + number}Circle.`);
    // Add the following for confirmation
    console.log(typeof(price));
    console.log(typeof(1));
  }